{
  "name": "minui",
  "version": "0.1.0",
  "description": "Custom in-game UI framework for Minecraft Bedrock add-ons: a screen language, compiler and runtime built on real, in-game-verified Bedrock mechanisms. Consumed by OpenChara (and any other project) as a sibling repo, not an npm package yet.",
  "private": true,
  "engines": { "node": ">=18" }
}
